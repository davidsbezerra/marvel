logging:
  level:
    root: WARN

server:
  port: 0

spring:
  application:
    name: api-marvel
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.amqp.RabbitAutoConfiguration
  main:
    banner-mode: "off"
  rabbitmq:
    connection-timeout: 1